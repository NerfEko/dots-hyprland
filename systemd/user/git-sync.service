[Unit]
Description=Git sync for ~/projects

[Service]
Type=oneshot
WorkingDirectory=%h/projects
ExecStart=/usr/bin/bash -lc 'set -e; git add -A; if ! git diff --cached --quiet; then git commit -m "Sync changes"; fi; git pull --rebase --autostash orgin master; git push orgin master'
