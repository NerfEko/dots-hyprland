input {
    sensitivity = -0.2
    accel_profile = flat
    kb_options = caps:super
}

misc {
    vrr = 2
    # Completely disable DPMS in Hyprland - let external tools handle it
    mouse_move_enables_dpms = false
    key_press_enables_dpms = false
}
